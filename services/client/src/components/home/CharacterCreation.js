import React from "react";

export default function CharacterCreation(props) {
    const user = props.user
    return <div className={"main-content"}>Select a character to show information here.</div>
}